/**
 * CHARACTER RENDERERS INDEX
 * Exports all character-specific rendering modules for client-side use
 */

const ValencinaRenderer = require('./valencina');
const CallistoRenderer = require('./callisto');

module.exports = {
  ValencinaRenderer,
  CallistoRenderer
};
