/**
 * RENDERING UTILITIES INDEX
 * Exports all shared rendering utilities for client-side use
 */

const Camera = require('./camera');
const ParticleSystem = require('./particles');
const VFX = require('./vfx');

module.exports = {
  Camera,
  ParticleSystem,
  VFX
};
