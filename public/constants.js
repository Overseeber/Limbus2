const ARENA_WIDTH = 1400;
const ARENA_HEIGHT = 700;
const GRAVITY = 0.6;
const FLOOR_Y = 100; // Match where characters actually stand (spawnY = height - 100, so floor is at y = 100 in arena coordinates)
const DEBUG = false;
const CAMERA_MARGIN = 1 / 7;